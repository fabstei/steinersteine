<h1>Petrtyp creation</h1>

<form action="{{ path('petrtyp_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('petrtyp') }}">
            Back to the list
        </a>
    </li>
</ul>
